name: Download Heureka Export

on:
  workflow_dispatch:
  schedule:
    - cron: '0 08 * * *'   # every day at 00:00 UTC
    - cron: '0 14 * * *'  # every day at 12:00 UTC

permissions:
  contents: write

jobs:
  download-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download file
        run: |
          curl -o heureka_reviews.xml "https://www.heureka.cz/direct/dotaznik/export-review.php?key=43470027a41b7d12cb8121f48a5b6369"

      - name: Commit and push changes
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add heureka_reviews.xml
          git commit -m "Update heureka_reviews.xml" || echo "No changes to commit"
          git push
